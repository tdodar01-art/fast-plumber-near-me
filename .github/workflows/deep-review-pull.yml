name: Deep Review Pull (Outscraper)

on:
  schedule:
    # 7 AM Central = 12:00 UTC (CDT) — 1 hour after main daily scrape
    - cron: '0 12 * * *'
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

jobs:
  deep-reviews:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: apps/plumbers-web

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install firebase-admin outscraper googleapis

      - name: Check secrets
        id: secrets
        run: |
          if [ -n "$FIREBASE_SERVICE_ACCOUNT" ] && [ -n "$OUTSCRAPER_API_KEY" ]; then
            echo "configured=true" >> $GITHUB_OUTPUT
          else
            echo "configured=false" >> $GITHUB_OUTPUT
            echo "Missing required secrets. FIREBASE_SERVICE_ACCOUNT or OUTSCRAPER_API_KEY not set."
          fi
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          OUTSCRAPER_API_KEY: ${{ secrets.OUTSCRAPER_API_KEY }}

      - name: Setup service account
        if: steps.secrets.outputs.configured == 'true'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: echo "$FIREBASE_SERVICE_ACCOUNT" > service-account.json

      # Query Firestore for high-traction cities, filter stale ones, cap at 3
      - name: Find qualifying cities
        if: steps.secrets.outputs.configured == 'true'
        id: cities
        run: |
          CITY_SLUGS=$(node -e "
            const admin = require('firebase-admin');
            const fs = require('fs');
            const sa = JSON.parse(fs.readFileSync('service-account.json', 'utf-8'));
            admin.initializeApp({ credential: admin.credential.cert(sa) });
            const db = admin.firestore();

            async function main() {
              // Debug: log all cities and their gscTier values
              const allCities = await db.collection('cities').get();
              console.error('DEBUG: Total cities in Firestore: ' + allCities.size);
              const tierCounts = {};
              for (const d of allCities.docs) {
                const tier = d.data().gscTier || '(missing)';
                tierCounts[tier] = (tierCounts[tier] || 0) + 1;
                if (tier === 'medium' || tier === 'high') {
                  console.error('  ' + d.id + ' → gscTier=' + tier + ', impressions=' + (d.data().lastGSCImpressions || 0));
                }
              }
              console.error('DEBUG: gscTier distribution: ' + JSON.stringify(tierCounts));

              // Get cities with medium or high GSC tier (10+ impressions)
              const snap = await db.collection('cities')
                .where('gscTier', 'in', ['medium', 'high'])
                .get();

              if (snap.empty) {
                console.error('No medium/high tier cities found. (See tier distribution above)');
                process.exit(0);
              }

              const thirtyDaysAgo = new Date();
              thirtyDaysAgo.setDate(thirtyDaysAgo.getDate() - 30);

              const candidates = [];
              for (const cityDoc of snap.docs) {
                const cityData = cityDoc.data();
                const fullSlug = cityDoc.id; // e.g. 'crystal-lake-il'

                // serviceCities stores city-only slugs (e.g. 'crystal-lake'), not 'crystal-lake-il'
                // Strip the state suffix to match
                const cityOnlySlug = fullSlug.replace(/-[a-z]{2}$/, '');

                // Try serviceCities match first, fall back to state-based query
                let plumberSnap = await db.collection('plumbers')
                  .where('serviceCities', 'array-contains', cityOnlySlug)
                  .where('isActive', '==', true)
                  .get();

                // Fallback: if serviceCities match is empty, query by state + active
                // (many plumbers don't have serviceCities populated)
                if (plumberSnap.empty) {
                  const stateAbbr = (fullSlug.match(/-([a-z]{2})$/)?.[1] || '').toUpperCase();
                  if (stateAbbr) {
                    const stateSnap = await db.collection('plumbers')
                      .where('address.state', '==', stateAbbr)
                      .where('isActive', '==', true)
                      .get();
                    // Filter to plumbers near this city (we don't have coords here,
                    // so just use all state plumbers — the outscraper script handles geo)
                    plumberSnap = stateSnap;
                    if (!stateSnap.empty) {
                      console.error('  ' + fullSlug + ': 0 serviceCities matches, using ' + stateSnap.size + ' state-level plumbers');
                    }
                  }
                }

                if (plumberSnap.empty) continue;

                let allFresh = true;
                for (const p of plumberSnap.docs) {
                  const lastPull = p.data().lastOutscraperPull;
                  if (!lastPull || lastPull.toDate() < thirtyDaysAgo) {
                    allFresh = false;
                    break;
                  }
                }

                if (!allFresh) {
                  candidates.push({
                    slug: citySlug,
                    impressions: cityData.lastGSCImpressions || 0,
                    tier: cityData.gscTier,
                    plumbers: plumberSnap.size,
                  });
                }
              }

              // Sort by impressions desc, cap at 3
              candidates.sort((a, b) => b.impressions - a.impressions);
              const selected = candidates.slice(0, 3);

              if (selected.length === 0) {
                console.error('All qualifying cities have fresh Outscraper data.');
                process.exit(0);
              }

              for (const c of selected) {
                console.error('Selected: ' + c.slug + ' (' + c.tier + ', ' + c.impressions + ' impressions, ' + c.plumbers + ' plumbers)');
              }

              // Output space-separated slugs
              console.log(selected.map(c => c.slug).join(' '));
            }
            main().catch(e => { console.error('FATAL: ' + e.message); console.error(e.stack); process.exit(1); });
          " 2>&1)

          # Extract just the last line (stdout) as city slugs — stderr goes to log
          SLUGS=$(echo "$CITY_SLUGS" | tail -1)
          echo "$CITY_SLUGS" | head -n -1  # print logs

          if [ -z "$SLUGS" ] || echo "$SLUGS" | grep -q "^No \|^All "; then
            echo "No cities to process."
            echo "slugs=" >> $GITHUB_OUTPUT
          else
            echo "Cities to process: $SLUGS"
            echo "slugs=$SLUGS" >> $GITHUB_OUTPUT
          fi

      # BBB lookup BEFORE reviews so synthesis has BBB data available
      - name: BBB lookup for qualifying cities
        if: steps.secrets.outputs.configured == 'true' && steps.cities.outputs.slugs != ''
        continue-on-error: true
        run: |
          for SLUG in ${{ steps.cities.outputs.slugs }}; do
            echo "BBB lookup for: $SLUG"
            node scripts/bbb-lookup.js "$SLUG" || true
            sleep 2
          done

      # Run outscraper-reviews.js for each city (one at a time for error isolation)
      - name: Pull reviews for city 1
        if: steps.secrets.outputs.configured == 'true' && steps.cities.outputs.slugs != ''
        continue-on-error: true
        env:
          OUTSCRAPER_API_KEY: ${{ secrets.OUTSCRAPER_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          SLUG=$(echo "${{ steps.cities.outputs.slugs }}" | awk '{print $1}')
          if [ -n "$SLUG" ]; then
            echo "Processing city 1: $SLUG"
            node scripts/outscraper-reviews.js "$SLUG"
          fi

      - name: Pull reviews for city 2
        if: steps.secrets.outputs.configured == 'true' && steps.cities.outputs.slugs != ''
        continue-on-error: true
        env:
          OUTSCRAPER_API_KEY: ${{ secrets.OUTSCRAPER_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          SLUG=$(echo "${{ steps.cities.outputs.slugs }}" | awk '{print $2}')
          if [ -n "$SLUG" ]; then
            echo "Processing city 2: $SLUG"
            node scripts/outscraper-reviews.js "$SLUG"
          fi

      - name: Pull reviews for city 3
        if: steps.secrets.outputs.configured == 'true' && steps.cities.outputs.slugs != ''
        continue-on-error: true
        env:
          OUTSCRAPER_API_KEY: ${{ secrets.OUTSCRAPER_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          SLUG=$(echo "${{ steps.cities.outputs.slugs }}" | awk '{print $3}')
          if [ -n "$SLUG" ]; then
            echo "Processing city 3: $SLUG"
            node scripts/outscraper-reviews.js "$SLUG"
          fi

      # Export Firestore enrichment to static JSON → commit → push → Vercel rebuild
      - name: Git setup and pull
        if: steps.secrets.outputs.configured == 'true' && steps.cities.outputs.slugs != ''
        continue-on-error: true
        working-directory: .
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase || true

      - name: Export Firestore to static JSON
        if: steps.secrets.outputs.configured == 'true' && steps.cities.outputs.slugs != ''
        continue-on-error: true
        id: export
        run: |
          node scripts/export-firestore-to-json.js | tee /tmp/export-output.txt

          # Parse affected cities for indexing step
          CITIES=$(grep '__AFFECTED_CITIES__:' /tmp/export-output.txt | sed 's/.*__AFFECTED_CITIES__://' || true)
          echo "affected_cities=$CITIES" >> $GITHUB_OUTPUT

      # Request indexing for ALL city pages served by updated plumbers
      - name: Request indexing for affected cities
        if: steps.secrets.outputs.configured == 'true' && steps.export.outputs.affected_cities != '' && steps.export.outputs.affected_cities != '[]'
        continue-on-error: true
        env:
          GSC_SITE_URL: https://www.fastplumbernearme.com/
        run: |
          npm install googleapis 2>/dev/null

          URLS=$(node -e "
            const fs = require('fs');
            const cities = ${{ steps.export.outputs.affected_cities || '[]' }};
            if (!cities.length) process.exit(0);
            const abbrs = {AL:'alabama',AK:'alaska',AZ:'arizona',AR:'arkansas',CA:'california',CO:'colorado',CT:'connecticut',DE:'delaware',FL:'florida',GA:'georgia',HI:'hawaii',ID:'idaho',IL:'illinois',IN:'indiana',IA:'iowa',KS:'kansas',KY:'kentucky',LA:'louisiana',ME:'maine',MD:'maryland',MA:'massachusetts',MI:'michigan',MN:'minnesota',MS:'mississippi',MO:'missouri',MT:'montana',NE:'nebraska',NV:'nevada',NH:'new-hampshire',NJ:'new-jersey',NM:'new-mexico',NY:'new-york',NC:'north-carolina',ND:'north-dakota',OH:'ohio',OK:'oklahoma',OR:'oregon',PA:'pennsylvania',RI:'rhode-island',SC:'south-carolina',SD:'south-dakota',TN:'tennessee',TX:'texas',UT:'utah',VT:'vermont',VA:'virginia',WA:'washington',WV:'west-virginia',WI:'wisconsin',WY:'wyoming',DC:'district-of-columbia'};
            const data = JSON.parse(fs.readFileSync('data/synthesized/plumbers-synthesized.json','utf-8'));
            const cityState = new Map();
            data.plumbers.forEach(p => (p.serviceCities||[]).forEach(c => { if (!cityState.has(c)) cityState.set(c, p.state); }));
            const urls = cities.map(c => {
              const state = cityState.get(c);
              if (!state) return null;
              const ss = abbrs[state] || state.toLowerCase();
              return '/emergency-plumbers/' + ss + '/' + c;
            }).filter(Boolean);
            if (urls.length) console.log(urls.join(' '));
          " 2>/dev/null)

          if [ -n "$URLS" ]; then
            echo "Requesting indexing for: $URLS"
            node scripts/request-indexing.js $URLS
          fi

      - name: Cleanup service account
        if: always()
        run: rm -f service-account.json
